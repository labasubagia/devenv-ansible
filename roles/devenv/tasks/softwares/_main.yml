---
- name: setup apps
  include_tasks: "{{ task }}"
  loop_control:
    loop_var: task
  with_items:
    - _apt.yml
    - _flatpak.yml
    - arcanist.yml
    - asdf.yml
    - docker.yml
    - mockoon.yml
    - vscode.yml
    - xdm.yml
    - gcloud-cli.yml